# WF-Guard Demo — Python 3.11
# Setup:
#   python -m venv .venv
#   source .venv/bin/activate        # Linux / WSL
#   pip install -r requirements.txt

# ── Dashboard ──────────────────────────────────────────────────────────────────
streamlit>=1.32,<2.0

# ── Data / ML ──────────────────────────────────────────────────────────────────
numpy>=1.24,<2.0
pandas>=2.0,<3.0
scikit-learn>=1.4,<2.0
joblib>=1.3

# ── Packet capture ─────────────────────────────────────────────────────────────
# Required for real-mode dashboard and scripts/collect/collect_fresh.py
# Needs CAP_NET_RAW: sudo setcap cap_net_raw+eip $(readlink -f .venv/bin/python)
scapy>=2.5

# ── Defense proxy / HTTP ───────────────────────────────────────────────────────
requests[socks]>=2.31
PySocks>=1.7
fake-useragent>=1.4

# ── Browser automation ─────────────────────────────────────────────────────────
# Required for traffic_gen.py and scripts/collect/collect_fresh.py
# Also needs: Firefox ESR + geckodriver in /usr/local/bin/ (see drivers/)
selenium>=4.18

# ── Tor circuit control (optional) ────────────────────────────────────────────
# Enables identity rotation between page loads; skipped gracefully if absent
stem>=1.8
